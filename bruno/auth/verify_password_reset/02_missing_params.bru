meta {
  name: missing params
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}{{apiBase}}/auth/password/reset/verify
  body: none
  auth: none
}

tests {
  test("status 422", function() {
    expect(res.status).to.equal(422);
  });

  test("success flag is false", function() {
    expect(res.body.success).to.be.false;
  });
}
