meta {
  name: validation error
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}{{apiBase}}/categories/{{categoryId}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  // missing required fields — name, url_slug, and collection_id are all absent
  {}
}

tests {
  test("status 422", function() {
    expect(res.status).to.equal(422);
  });

  test("error code is UNPROCESSABLE_ENTITY", function() {
    expect(res.body.error.code).to.equal("UNPROCESSABLE_ENTITY");
  });
}
